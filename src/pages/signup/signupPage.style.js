import styled from 'styled-components';

const SignupWrapper = styled.div`
  //refactor, these h3, h4 should have prop types accessible in the component
  h3 {
    text-align: center;
    margin: 30px;
  }
`;

export default SignupWrapper;
